{%- comment -%}
Dedicated Car Parts Search Page Template
Use this template for pages that need just the car parts search functionality
{%- endcomment -%}

{%- style -%}
.search-parts-page {
  padding: 40px 0 80px;
  min-height: 70vh;
}

.search-parts-container {
  max-width: 1000px;
  margin: 0 auto;
  padding: 0 20px;
}

.search-parts-header {
  text-align: center;
  margin-bottom: 40px;
}

.search-parts-title {
  font-size: 3rem;
  font-weight: 700;
  color: var(--color-text);
  margin-bottom: 20px;
  line-height: 1.2;
}

.search-parts-subtitle {
  font-size: 1.3rem;
  color: var(--color-text-light);
  max-width: 600px;
  margin: 0 auto 30px;
  line-height: 1.5;
}

.search-tips {
  background: rgba(211, 47, 47, 0.05);
  border: 2px solid var(--color-primary-light);
  border-radius: 12px;
  padding: 30px;
  margin-top: 50px;
  text-align: center;
}

.search-tips h3 {
  color: var(--color-primary);
  font-size: 1.5rem;
  margin-bottom: 20px;
}

.search-tips-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 30px;
  margin-top: 30px;
}

.search-tip {
  text-align: center;
}

.search-tip-icon {
  font-size: 2.5rem;
  margin-bottom: 15px;
  display: block;
}

.search-tip h4 {
  color: var(--color-primary);
  font-size: 1.2rem;
  margin-bottom: 10px;
}

.search-tip p {
  color: var(--color-text-light);
  line-height: 1.5;
}

@media (max-width: 768px) {
  .search-parts-title {
    font-size: 2.5rem;
  }
  
  .search-parts-subtitle {
    font-size: 1.1rem;
  }
  
  .search-tips {
    padding: 20px;
  }
  
  .search-tips-grid {
    grid-template-columns: 1fr;
    gap: 25px;
  }
}
{%- endstyle -%}

<div class="search-parts-page">
  <div class="search-parts-container">
    
    <div class="search-parts-header">
      <h1 class="search-parts-title">{{ page.title | default: "Търси автомобилни части" }}</h1>
      <p class="search-parts-subtitle">
        {{ page.content | default: "Намери точните части за своя автомобил бързо и лесно. Използвай ръчно търсене или въведи VIN номера на колата си." }}
      </p>
    </div>

    <!-- Main Car Parts Search Component -->
    {% section 'car-parts-search' %}

    <!-- Search Tips Section -->
    <div class="search-tips">
      <h3>💡 Съвети за търсене</h3>
      <p>Следвай тези съвети за най-добри резултати при търсенето на части:</p>
      
      <div class="search-tips-grid">
        <div class="search-tip">
          <span class="search-tip-icon">🗓️</span>
          <h4>Точна година</h4>
          <p>Избери точната година на производство на автомобила си за най-прецизни резултати.</p>
        </div>
        
        <div class="search-tip">
          <span class="search-tip-icon">🚗</span>
          <h4>Правилна марка</h4>
          <p>Уверете се, че сте избрали правилната марка и модел за съвместимост на частите.</p>
        </div>
        
        <div class="search-tip">
          <span class="search-tip-icon">🔢</span>
          <h4>Използвай VIN</h4>
          <p>VIN номерът дава най-точна информация за автомобила и гарантира съвместимост.</p>
        </div>
      </div>
    </div>

  </div>
</div> 